# webpack-post-bundle-commands
Post Commands for Webpack

Useage:
```
new RunKarmaAfterBundle([
  "pwd",
  ["ls", ["-l"]]
])
```
As seen above there are two options. The first runs a simple command, and the second runs a command with arguements.
